# Node
node_modules/
npm-debug.log
yarn-error.log
# Environment variables
.env
.env.local
.env.*.local
.env.development
.env.test
.env.production

# Optional: keep a template but ignore real values
# .env.example  # (do NOT add this line if you want to commit .env.example)

# Build outputs (adjust if your paths differ)
dist/
build/